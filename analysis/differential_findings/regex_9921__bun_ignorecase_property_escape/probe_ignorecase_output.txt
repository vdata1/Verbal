# Spec-conformance probe for /\p{...}/ under /i. Pinned engines (verbal:8dc8b1d).
# See DISCREPANCIES.md#f003 (originally EXPERIMENT_GAPS.md G3b, before the finding
# was catalogued). Expected values derive from ES2024 22.2.2.7.1.
#
# Re-verified 2026-07-15 on verbal:d1f3125 (the image that produced this folder's
# pipeline artifacts): output is byte-identical. The engines are the same across both
# tags -- node v26.5.0, bun 1.3.14, deno 2.9.1 -- only the pipeline code differs.

===== node =====
  pass  /\p{Lu}/iu.test("a")   -- scf("a")="a", "A" in Lu folds to "a"
  pass  /\p{Ll}/iu.test("A")   -- mirror case
  pass  /\p{Lu}/iu.test("A")   -- trivially true
  pass  /\p{Lu}/u.test("a")    -- CONTROL: no /i, must be false
  pass  /\p{Ll}/u.test("A")    -- CONTROL: no /i, must be false
  pass  /[A-Z]/iu.test("a")     -- CONTROL: classic range folding
  pass  /[a-z]/iu.test("A")     -- CONTROL: classic range folding
  pass  /\p{Lu}/vi.test("a")   -- v flag instead of u
  pass  /\p{Uppercase_Letter}/iu.test("a") -- long name
  pass  /[\p{Lu}]/iu.test("a") -- inside a class
  pass  /\p{Lu}/iu.test("é")   -- non-ASCII BMP (É is Lu)
  --> 11/11 match the spec

===== bun =====
  FAIL  /\p{Lu}/iu.test("a")   -- scf("a")="a", "A" in Lu folds to "a"
          expected true, got false
  FAIL  /\p{Ll}/iu.test("A")   -- mirror case
          expected true, got false
  pass  /\p{Lu}/iu.test("A")   -- trivially true
  pass  /\p{Lu}/u.test("a")    -- CONTROL: no /i, must be false
  pass  /\p{Ll}/u.test("A")    -- CONTROL: no /i, must be false
  pass  /[A-Z]/iu.test("a")     -- CONTROL: classic range folding
  pass  /[a-z]/iu.test("A")     -- CONTROL: classic range folding
  FAIL  /\p{Lu}/vi.test("a")   -- v flag instead of u
          expected true, got false
  FAIL  /\p{Uppercase_Letter}/iu.test("a") -- long name
          expected true, got false
  FAIL  /[\p{Lu}]/iu.test("a") -- inside a class
          expected true, got false
  FAIL  /\p{Lu}/iu.test("é")   -- non-ASCII BMP (É is Lu)
          expected true, got false
  --> 5/11 match the spec

===== deno =====
  pass  /\p{Lu}/iu.test("a")   -- scf("a")="a", "A" in Lu folds to "a"
  pass  /\p{Ll}/iu.test("A")   -- mirror case
  pass  /\p{Lu}/iu.test("A")   -- trivially true
  pass  /\p{Lu}/u.test("a")    -- CONTROL: no /i, must be false
  pass  /\p{Ll}/u.test("A")    -- CONTROL: no /i, must be false
  pass  /[A-Z]/iu.test("a")     -- CONTROL: classic range folding
  pass  /[a-z]/iu.test("A")     -- CONTROL: classic range folding
  pass  /\p{Lu}/vi.test("a")   -- v flag instead of u
  pass  /\p{Uppercase_Letter}/iu.test("a") -- long name
  pass  /[\p{Lu}]/iu.test("a") -- inside a class
  pass  /\p{Lu}/iu.test("é")   -- non-ASCII BMP (É is Lu)
  --> 11/11 match the spec

